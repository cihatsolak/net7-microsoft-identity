@model List<UserViewModel>
@{
    ViewData["Title"] = "Users";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}


@if (0 >= Model.Count)
{
    <div class="alert alert-warning" role="alert">
        Users Not Found!
    </div>
}
else
{
    <h4>Members</h4>
    <hr />
    <table class="table table-bordered table-striped table-responsive">
        <thead>
            <tr>
                <th scope="col">City Name</th>
                <th scope="col">Username</th>
                <th scope="col">Email</th>
                <th scope="col">Roles</th>
                <th scope="col">Process</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var user in Model)
            {
            <tr>
                <td>@user.CityName</td>
                <td>@user.UserName</td>
                <td>@user.Email</td>
                <td>
                    <user-roles user-id="@user.Id"></user-roles>
                </td>
                <td>
                    <a class="btn btn-success btn-sm"
                       asp-controller="Role" asp-action="Assign" asp-route-userId="@user.Id">Assign</a>

                    <a class="btn btn-info btn-sm"
                       asp-controller="Admin" asp-action="ChangePasswordForMember" asp-route-userId="@user.Id">Change Password</a>
                </td>
            </tr>
            }
        </tbody>
    </table>
}

