@model List<RoleViewModel> 
@{
    ViewData["Title"] = "Roles";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<a class="btn btn-success my-3" asp-action="Create">Role Oluştur</a>

@if (0 >= Model.Count)
{
    <div class="alert alert-warning" role="alert">
        Roles Not Found!
    </div>
}
else
{
    <h4>Roles</h4>
    <hr />
    <table class="table table-bordered table-striped">
        <thead>
            <tr>
                <th scope="col">Role Name</th>
                <th scope="col">Role Process</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var role in Model)
            {
                <tr>
                    <td>@role.Name</td>
                    <td>
                        <div class="row">
                            <div class="col-md-2">
                                <form asp-action="Delete" asp-route-roleId="@role.Id" method="post">
                                    <button class="btn btn-danger btn-sm" type="submit">Delete</button>
                                </form>
                            </div>
                            <div class="col-md-2">
                                <a class="btn btn-info btn-sm" asp-action="Edit" asp-route-roleId="@role.Id">Edit</a>
                            </div>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}